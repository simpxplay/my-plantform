# URL of the Laravel backend accessible from your device/emulator
# For ddev: use your machine's local IP + port 8080 (http, NOT https — phones don't trust the ddev cert)
# Find your IP: hostname -I | awk '{print $1}'
API_URL=http://192.168.x.x:8080
